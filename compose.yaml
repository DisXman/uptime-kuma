services:
  uptime-kuma:
    build:
      context: .
      dockerfile: docker/dockerfile
    container_name: uptime-kuma
    restart: unless-stopped
    ports:
      - "3001:3001"
    volumes:
      - ./data:/app/data
